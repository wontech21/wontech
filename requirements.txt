# FIRINGup Inventory Management System
# Python Dependencies

# Core Framework
Flask==3.1.2

# Flask automatically installs these dependencies:
# - Werkzeug (WSGI utility library)
# - Jinja2 (template engine)
# - itsdangerous (secure signing)
# - click (CLI creation)
# - blinker (signals support)
# - MarkupSafe (string escaping)

# HTTP Requests (for barcode API lookups)
requests==2.31.0

# Database
# SQLite3 is included with Python (no package needed)

# Production server
gunicorn==21.2.0
